namespace LearnProgramming.Operator;

public class ArithmeticOperators
{
    //TODO:Açıklama ve örnek ekle.
    //TODO: Türkçe ve İngilizce ekle.
    
    // sayısal işlemler için kullanılan operatörlerdir.
    /*
     -Unary ++ (increment), -- (decrement), + (plus), and - (minus) operators
       -Binary * (multiplication), / (division), % (remainder), + (addition), and - (subtraction) operators
     */
    
    // Bu operatörler integral ve floating-point sayılar üzerinde çalışır.
    
    
    /*
     * Aritmetik operatörler
     -Aritmetik operatörler, kullanıcı tanımlı(user-defined) tiplerde aşırı yüklenebilir.
       Aşırı yükleme işleminde checked anahtar sözcüğü, işlem sırasında taşma kontrolü için kullanılabilir.
     */
    
    
    /* İSTİSNA-Interview Question
     * byte ve byte ile işlem sonucu int olur.
     */
}